info.py             Statistical characteristics of PPI network  PPI网络的网络统计特征
eval.py             Precision evaluation of AROPE algorithm     AROPE算法的精准度评价        
extract.py          Extract the first two columns of *.txt and append a column with a value of 1                            提取txt文档的前两列,并追加数值都是1的一列.
normal.py           Weight normalization of links 链接上的权重归一化
S.pombe_AROPE.py    AROPE algorithm on PPI network S.pombe
S.pombe_draw.py     Draw precision curves of methods
S.pombe_L3.py       L3 algorithm on PPI network S.pombe
S.pombe_LO.py       LO algorithm on PPI network S.pombe
S.pombe_Rank.py     Integration of methods, including L3+Sim
S.pombe_Sim.py      Sim algorithm on PPI network S.pombe
S.pombe_SimCN.py    SimCN algorithm on PPI network S.pombe
S.pombe_SimPA.py    SimPA algorithm on PPI network S.pombe
S.pombe_SimRA.py    SimRA algorithm on PPI network S.pombe
sample.py           Divide the edge set of the network into 10 subsets for 10 fold cross validation 十折交叉验证前采样边集
utils.py            network embeding for AROPE algorithm  网络嵌入
weight.py           weight every links of a unweighted network 给无权的网络赋权
weightless.py       deleting links with small weight 删除权小的链接
